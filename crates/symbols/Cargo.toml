[package]
name = "tine_symbols"
version.workspace = true
edition = "2021"
build = "build.rs"

[dependencies]
tine_common = { path = "../common" }
tine_macros = { path = "../macros" }
tine_types = { path = "../types" }