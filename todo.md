- accept dynamic in some expressions? `if some { [""] } else { [] }`

- if decl: accept ':='
- match expression
  - need to check if all cases are handled
  - sum types: check if all constructors are used
- function expressions
- handle return in functions (check nested blocks)
- function call
