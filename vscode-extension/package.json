{
  "name": "vscode-extension",
  "displayName": "Tine",
  "description": "VS Code support for Tine",
  "version": "0.1.0",
  "publisher": "bmelicque",
  "engines": {
    "vscode": "^1.105.0"
  },
  "main": "./out/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "tine",
        "aliases": [
          "Tine"
        ],
        "extensions": [
          ".tine"
        ],
        "configuration": "./language-configuration.json"
      },
      {
        "id": "tine-types",
        "extensions": []
      }
    ],
    "grammars": [
      {
        "language": "tine",
        "scopeName": "source.tine",
        "path": "./syntaxes/tine.tmLanguage.json"
      },
      {
        "language": "tine-types",
        "scopeName": "source.tine-types",
        "path": "./syntaxes/tinetypes.tmLanguage.json"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npx tsc -p ./",
    "compile": "npx tsc -p ./",
    "watch": "npx tsc -watch -p ./",
    "build:server": "cd ../crates/server && cargo build --release",
    "copy:server": "mkdir -p ./server/bin && cp ../target/release/tine_server ./server/bin/",
    "copy:server:win": "mkdir server\\bin 2> NUL & copy ..\\target\\release\\tine_server.exe server\\bin\\",
    "build:extension": "vsce package",
    "install:extension": "node -e \"const fs=require('fs');const{execSync}=require('child_process');const vsix=fs.readdirSync('.').find(f=>f.endsWith('.vsix'));if(!vsix)throw new Error('No VSIX found');execSync(`code --install-extension ${vsix}`,{stdio:'inherit'})\"",
    "build:all": "npm run build:server && npm run copy:server:win && npm run build:extension && npm run install:extension"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^24.10.0",
    "@types/vscode": "^1.105.0",
    "@vscode/test-electron": "^2.5.2",
    "typescript": "^5.9.3",
    "vscode-test": "^1.6.1"
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  }
}